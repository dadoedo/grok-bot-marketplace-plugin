# X / hosted feed env

# Published plugin: no X API key. It reads the public feed.
# GROKBOTS_FEED_URL=https://grokbots.store/feed.json
# GROKBOTS_OFFLINE=1

# Operator / hetzner-prod only (ops/refresh_feed.py). Never commit this file.
# X_BEARER_TOKEN=
# TWITTER_BEARER_TOKEN=
# X_API_BEARER_TOKEN=

# CI / local fixture for X client tests (not the product path):
# X_DEMO=1
