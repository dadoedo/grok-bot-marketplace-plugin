# grokbots.store feed — hetzner-prod only (stredan-cursor-agent@46.224.84.45)
#   crontab ops/crontab.example
# Copy ops/env.example → ~/grokbots/.env (chmod 600) first.
# Do NOT run this cron on the Grok Bot agent box.

SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin

# Current: refresh marketplace (+ X if Bearer is set); do not upload until R2 tokens exist
*/30 * * * * SKIP_PUBLISH=1 /home/stredan-cursor-agent/grokbots/ops/cron-refresh.sh

# After CLOUDFLARE_API_TOKEN or AWS R2 keys are in ~/grokbots/.env, replace with:
# */30 * * * * /home/stredan-cursor-agent/grokbots/ops/cron-refresh.sh
